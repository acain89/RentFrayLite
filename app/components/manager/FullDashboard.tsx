"use client";

type Props = {
  data: any;
};

export default function FullDashboard({ data }: Props) {
  // 👉 TEMP: render existing content

  return (
    <div>
      {/* MOVE YOUR CURRENT DASHBOARD JSX HERE */}
      <pre style={{ color: "#fff" }}>
        Full Dashboard Placeholder — paste existing UI here
      </pre>
    </div>
  );
}